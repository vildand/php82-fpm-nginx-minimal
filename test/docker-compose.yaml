version: '3'
services:
  fpm:
    build: ..
    volumes:
      - ./index.php:/var/www/html/index.php
  web:
    image: nginx:alpine
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 8080:80